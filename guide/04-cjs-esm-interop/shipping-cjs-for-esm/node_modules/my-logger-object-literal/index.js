class Logger {
  constructor(level) {
    this.level = level;
  }

  log(message) {
    console.log(`[${this.level}] ${message}`);
  }
}

module.exports = {
  Logger,  // This can be detected as a named export
};
